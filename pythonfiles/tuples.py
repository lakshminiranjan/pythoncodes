#!/bin/python3
#Tuples-are "IMUTABLE"-have peranthesis()
grades = ("a" , "b" , "c" , "d" , "f" )
print(grades[1])#prints the second element i.e., "b".
print(grades[0 :])
#Everything works similar to list the only change is instead of brackets we use peranthesis and cant append or pop a element.
